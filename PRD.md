# DA数据清洗业务AI应用 — 产品需求文档 (PRD)

> **版本**: v1.4.0  
> **状态**: 开发中  
> **最后更新**: 2026-07-09  

---

## 1. 产品背景

### 1.1 现状与痛点

财务人员在日常工作中需要频繁查询和分析序时账（General Ledger）数据，当前主流工作方式存在以下问题：

| 痛点 | 具体表现 |
|------|---------|
| **操作繁琐** | 每次查询需打开 Excel → 筛选 → 透视表 → 写公式，一个简单问题耗时 5-15 分钟 |
| **技能门槛高** | 需要掌握 Excel 高级功能（VLOOKUP、透视表、公式嵌套）或 SQL 技能 |
| **重复劳动** | 类似的查询需求反复执行（"某某科目的本月发生额"、"某部门的费用汇总"），每次手动操作 |
| **数据验证困难** | 需要人工核对序时账与科目余额表的一致性，缺乏自动化验证手段 |
| **安全顾虑** | 财务数据敏感，企业不允许上传至公有云服务 |
| **工具割裂** | 分析过程中需要在 Excel、数据库客户端、计算器等多个工具之间切换 |

### 1.2 机会

大语言模型（LLM）在代码生成领域已展现足够能力，可以将自然语言查询直接翻译为 DuckDB SQL，在本地安全执行。Dify Workflow 代理 AI 调用，支持多模型路由。

### 1.3 产品定位

> **本地部署、AI 驱动、自然语言交互的财务数据查询分析工具**  
> 让非技术用户通过说中文就能查询财务数据，无需编写任何代码。

---

## 2. 目标用户

| 用户画像 | 特征 | 核心需求 |
|---------|------|---------|
| **财务核算人员** | 每天处理凭证、账务，熟悉科目结构但非技术背景 | 快速按科目/部门/时间筛选汇总数据 |
| **财务主管/经理** | 需要定期查看财务报表和异常数据 | 数据完整性验证、交叉比对、异常检测 |
| **审计人员** | 需要抽样检查凭证和数据一致性 | 按条件筛选特定凭证、验证借贷平衡 |
| **中小企业主** | 不熟悉专业财务软件，但有基础的 Excel 数据 | 用大白话问"看看上个月花了多少钱" |

---

## 3. 功能需求

### 3.1 功能清单

| 模块 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| **数据导入** | 序时账上传 | P0 | 支持 xlsx/csv，自动检测编码（UTF-8/GBK/latin-1）和字段类型，基于 DuckDB 引擎，大文件高性能 |
| | 科目余额表上传 | P1 | 可选，多 Sheet 选择，用于完整性测试和交叉验证 |
| | 文件预览 | P0 | 上传前预览前 15 行原始数据，支持选择 Sheet 和表头行 |
| **字段映射** | 智能推荐映射 | P0 | 基于列名关键词自动匹配标准字段 |
| | AI 智能映射 | P0 | 调用 AI 根据列名+类型+样本值推荐映射 |
| | 手动字段映射 | P0 | 下拉选择源字段→标准字段，支持手动填写常数值（如公司名、币种） |
| | 双表映射 | P1 | 同时支持序时账和科目余额表两套标准字段映射 |
| | 映射历史记录 | P1 | 自动保存映射关系，同文件再次上传时可一键应用上次映射 |
| **AI 配置** | API 密钥管理 | P0 | Dify Workflow API Key / DeepSeek API Key，会话级存储，不持久化 |
| **完整性测试** | 4 步引导配置 | P1 | 预处理（向下填充/剔除规则）→ 金额处理（借正贷负/借贷方）→ 特殊调整（反结转/末级科目）→ 运行检查 |
| | AI 智能建议 | P1 | 自动分析数据特征（空值警告、父子层级检测），预填推荐配置 |
| | 序时账完整性测试 | P1 | 按凭证分组检查发生额分布，正数/负数/零值分组统计 |
| | 科目余额表完整性测试 | P1 | 汇总期初/期末，检查发生额归零 |
| | 交叉验证 | P1 | 两表按科目比对一致性，逐科目差异明细 |
| | AI 差异分析 | P2 | 自动分析异常模式，从审计视角给出原因和建议 |
| | AI 对话助手 | P2 | 多轮对话引导用户配置测试参数，解读结果 |
| | 测试结果导出 Excel | P2 | 3 sheet 全量明细导出（序时账/科目余额表/交叉验证） |
| | 反结转处理 | P1 | 自定义本年利润科目号/摘要关键词，自动剔除结转损益凭证 |
| | 末级科目筛选 | P1 | 自动识别科目父子层级，仅用末级科目参与计算 |
| | 币种筛选 | P1 | 按币种过滤科目余额表，支持多币种场景 |
| | 向下填充 | P2 | 凭证号/日期/制单人按凭证分组向下填充（处理首行有值其余空的情况） |
| **财务报表清洗** | 报表文件上传 | P1 | 支持 xlsx/xls，多 Sheet 选择 |
| | AI 结构识别 | P1 | 读前 10 行，自动判断报表类型/列映射/布局 |
| | 规则提取 | P1 | 全规则引擎（不调 AI），处理左右分栏/汇总行/签名行 |
| | 多报表导入 | P2 | 支持资产负债表+利润表分别导入后合并 |
| **科目余额表核对** | 科目→报表项目映射 | P1 | 规则引擎（编号前缀+名称关键词 100+ 规则）+ AI 兜底 |
| | 可编辑映射界面 | P1 | 双栏布局，鼠标修改映射关系，实时刷新重算差异 |
| | 同类科目分组 | P1 | 自动按科目类型归类（资产/负债/权益/收入/费用） |
| | 模糊匹配对齐 | P1 | 自动将标准科目名匹配到报表实际名称（含中文编号前缀/异体字） |
| | AI 差异分析 | P2 | 分析差异模式，自动发现映射调换/归属错误/遗漏科目 |
| | 一键优化映射 | P2 | 贪心爬山算法，自动调整科目→报表项目映射以最小化总差异 |
| | 核对结果导出 | P2 | 导出带差异标识、匹配科目明细的 Excel |
| **智能查询** | 自然语言查询输入 | P0 | 支持中文自然语言描述分析需求 |
| | AI 生成 DuckDB SQL | P0 | 调用 AI API 生成可执行 SQL |
| | AI 查询语义优化 | P1 | 模糊概念变具体（月底→最后五天）、关键词中英文扩展（调整→adj/adjustment）、人名拼音变体（周健→zhoujian/zj） |
| | 默认全字段呈现 | P0 | 用户未指定字段时默认 SELECT * 呈现全部列 |
| | SQL 安全执行 | P0 | DuckDB 只读模式 + SELECT 白名单校验 |
| | SQL 手动编辑 | P1 | CodeMirror 编辑器，支持修改 WHERE 条件 |
| | AI 代码复核 | P1 | 第二 AI 模型审查语法/安全/意图/性能四维复核 |
| | SQL 代码解释 | P1 | 逐行解释查询逻辑，辅助非技术用户理解 |
| | 查询历史 | P1 | 自动保存前 50 条查询记录，支持回溯 |
| | 收藏查询 | P1 | 收藏常用查询及结果，支持批量导出为多 Sheet Excel |
| | 结果导出 | P2 | CSV / Excel 导出 |
| **预设筛选规则** | 通用筛选规则包 | P1 | 内置大额交易检测、整月无凭证检测、借贷不平衡等通用规则 |
| | 行业异常特征包 | P1 | 内置针对服务/贸易/房地产/制造/建筑等行业的异常特征规则 |
| | EY GAM 异常筛选包 | P1 | 内置安永 GAM 方法论异常凭证筛选规则（13 条） |
| | 自定义规则 | P2 | 用户自定义 SQL 模板规则，支持保存/编辑/删除 |
| **取样方法** | 随机抽样 | P2 | 按设定比例/数量随机抽取凭证 |
| | 分层抽样 | P2 | 按科目/金额区间分层抽取 |
| | 大额优先抽样 | P2 | 按金额排序取前 N 笔 |
| | 异常条件抽样 | P2 | 按异常条件（如周末记账、整金额、高频科目）筛选抽样 |
| **UI/UX** | 步骤进度条 | P0 | 上传→映射→完整性测试→查询 四步引导 |
| | 大数据量表格 | P2 | 支持长列表滚动，分页加载 |

### 3.2 标准字段定义

#### 序时账标准字段（13 个）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 日期 | date | 是 | 交易发生日期 |
| 摘要 | text | 是 | 交易内容摘要 |
| 科目编号 | text | 是 | 会计科目编号 |
| 科目名称 | text | 是 | 会计科目名称 |
| 金额 | number | 是 | 交易发生额 |
| 凭证号 | text | 是 | 凭证编号 |
| 公司名 | text | 否 | 公司名称 |
| 借方 | number | 否 | 借方金额 |
| 贷方 | number | 否 | 贷方金额 |
| 部门 | text | 否 | 所属部门 |
| 制单人 | text | 否 | 制单人员 |
| 方向 | text | 否 | 借贷方向（借/贷），用于借正贷负金额调整 |
| 币种 | text | 否 | 币种，用于完整性测试多币种筛选 |

#### 科目余额表标准字段（12 个）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 公司名 | text | 是 | 公司名称 |
| 科目编号 | text | 是 | 会计科目编号 |
| 科目名称 | text | 是 | 会计科目名称 |
| 期初余额 | number | 是 | 期初余额 |
| 期末余额 | number | 是 | 期末余额 |
| 期初借方 | number | 否 | 期初借方余额（借贷方格式时用） |
| 期初贷方 | number | 否 | 期初贷方余额 |
| 期末借方 | number | 否 | 期末借方余额 |
| 期末贷方 | number | 否 | 期末贷方余额 |
| 期初余额方向 | text | 否 | 期初余额方向（借/贷） |
| 期末余额方向 | text | 否 | 期末余额方向（借/贷） |
| 币种 | text | 否 | 币种，用于完整性测试多币种筛选 |

---

## 4. 用户工作流

### 4.1 主流程

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         DA数据清洗业务AI应用 工作流                                        │
│                                                                                 │
│   ┌──────┐   ┌──────┐   ┌──────┐   ┌──────┐   ┌──────────┐   ┌──────────┐      │
│   │上传  │ → │API   │ → │字段  │ → │完整性│ → │财务报表  │ → │科目余额  │      │
│   │文件  │   │配置  │   │映射  │   │测试  │   │清洗      │   │表核对    │      │
│   └──────┘   └──────┘   └──────┘   └──────┘   └──────────┘   └──────────┘      │
│      ↓          ↓          ↓          ↓           ↓              ↓              │
│  上传Excel   输入Key    映射字段    一键运行    上传报表      规则映射+可        │
│  +科目余额   (DeepSeek) 到标准名称  (含反结转   AI读前10行    编辑映射+         │
│  表(可选)                          末级科目)   识别结构      金额核对            │
│                                                                                 │
│   ┌──────┐                                                                      │
│   │查询  │ ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─              │
│   │分析  │    (完整性测试和财务报表清洗可选，不经过也可直接查询)                    │
│   └──────┘                                                                      │
│      ↓                                                                          │
│  自然语言查询+语义优化                                                            │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 异常流程

**上传失败** → 提示不支持的文件类型 / 编码检测失败 → 自动尝试 GBK→UTF-8 转换，仍失败则建议用户另存为 UTF-8 CSV

**映射字段缺失** → 标红必填字段，阻止进入下一步 → 提示"X 是必填字段"

**AI 代码生成失败** → 返回具体错误（API Key 无效 / Dify 不可达 / 模型返回异常）

**代码执行失败** → 显示 DuckDB 原生错误提示，含出错行号和上下文

---

## 5. 技术需求

### 5.1 架构要求

- **纯本地部署**：所有数据在用户机器上处理，不上传任何第三方服务器（除 AI API 调用）
- **无数据库依赖**：使用 DuckDB 列式引擎做数据分析 + Flask-Session 文件存储，无需安装 MySQL/PostgreSQL
- **单文件启动**：`python app.py` 即可运行
- **DuckDB 引擎**：所有数据导入和查询走 DuckDB，不经过 pandas 加载（上传预览/分析阶段由 DuckDB 原生 CSV 读取器处理）
- **Dify 代理架构**：所有 AI 调用通过 Dify Workflow 代理（主模型 Qwen3-235B-A3B，复核模型 Qwen3-30B-A3B），也可通过 DeepSeek API 直连兜底

### 5.2 安全要求

- **SQL 安全执行**：所有查询通过 DuckDB 执行，代码层做 SELECT 白名单校验（禁止 INSERT/UPDATE/DELETE/DROP）
- **会话语义隔离**：每个浏览器会话独立 DuckDB 数据库文件（`temp/db/{session_id}.db`），无交叉访问
- **API Key 安全**：仅存于服务端 session 文件，不写日志、不入库
- **临时文件清理**：启动时清理超过 24 小时的临时文件和 DuckDB 数据库，会话结束时清理 XLSX 缓存 CSV

### 5.3 兼容性要求

- **操作系统**：macOS / Windows / Linux
- **Python**：3.9+
- **浏览器**：Chrome 90+ / Edge 90+ / Safari 14+ / Firefox 90+
- **文件格式**：xlsx / csv（自动检测编码：UTF-8 / UTF-8 BOM / GBK / latin-1，DuckDB 原生读 UTF-8/latin-1/UTF-16，GBK 自动转 UTF-8）

### 5.4 性能要求

| 指标 | 目标 |
|------|------|
| 文件上传解析 | < 5s（100 万行以内，DuckDB 临时表加载 + Python 层类型推断） |
| 字段映射保存 | < 1s |
| AI 代码生成 | < 10s（取决于 AI API 响应） |
| 代码执行 | < 30s（默认超时） |
| 完整性测试 | < 10s（100 万行以内） |
| Excel 导出 | < 5s（10 万行以内） |

---

## 6. 非功能需求

### 6.1 易用性
- 5 步引导式流程，步骤进度条清晰显示当前位置
- 字段映射支持智能推荐（基于关键词模糊匹配）
- 查询页提供常用示例按钮，一键填充
- 错误提示使用中文，对常见错误提供解决建议

### 6.2 可维护性
- 模块化设计：`modules/` 下每个模块职责单一
- 后端路由与业务逻辑分离：`app.py` 仅做路由编排，业务逻辑委托给模块
- 配置集中管理：`config.py`

### 6.3 可扩展性
- 新增数据源：扩展 `DuckDBEngine` / `AnalysisEngine` 支持新文件格式
- 新增完整性测试：在 `IntegrityChecker` 中添加新方法
- 切换 AI 模型：替换 Dify Workflow 后端模型或通过环境变量 `DEEPSEEK_API_KEY` 直连切换

---

## 7. 成功指标

| 指标 | 衡量方式 | 目标值 |
|------|---------|--------|
| 任务完成率 | 从上传到成功查询的完整流程 | > 80% |
| 平均查询时间 | 从输入自然语言到看到结果 | < 15s |
| 代码首次执行成功率 | 第一次执行无报错的比例 | > 70% |
| 字段映射准确率 | 智能推荐的正确率 | > 60% |

---

## 8. 边界与限制

### 8.1 当前版本不支持的场景
- 不支持多用户并发（Flask 开发服务器为单线程）
- 不支持跨会话数据持久化（无数据库，但映射历史可通过文件复用）
- 不支持图表可视化
- 不支持多文件联合查询（但支持序时账+科目余额表双表完整性测试和交叉验证）

### 8.2 已知限制
- AI SQL 生成质量依赖 AI 模型能力，复杂查询可能需要手动调整 SQL
- 超大文件（百万行以上）的完整性测试执行时间与数据量成正比
- 不含科目余额表时，测试二和测试三自动跳过
- CSV 文件编码自动检测基于前 1KB 采样，极端情况可能误判（降级为 latin-1）
- Dify Workflow 不可达时自动降级为 DeepSeek API 直连（需配置 `DEEPSEEK_API_KEY` 环境变量）

---

## 9. 竞品分析

| 竞品 | 优势 | 劣势 |
|------|------|------|
| Excel 手动操作 | 无需额外工具、通用性强 | 效率低、技能门槛高、易出错 |
| Python/Jupyter | 灵活强大 | 需要编程能力 |
| 商业 BI 工具 (Power BI/Tableau) | 可视化强、功能完善 | 价格高、部署复杂、数据需上传 |
| DA数据清洗业务AI应用 | 自然语言驱动、本地安全、零代码 | 功能聚焦查询分析、不包含可视化 |

---

## 10. 路线图

### v1.3.0（已发布）
- 单文件序时账导入 + 字段映射
- DeepSeek AI 代码生成 + 沙箱执行
- 三项完整性测试
- 科目余额表导入 + 交叉验证
- 财务报表清洗（AI 读前 10 行识别结构 + 规则提取）
- 科目余额表 ↔ 报表核对（规则映射 + 可编辑 + AI 差异分析）
- 多报表导入合并核对
- 结果导出 Excel/CSV

### v1.4.0（当前）
- **DuckDB 上传分析重构**：上传预览/分析阶段从 pandas 迁移到 DuckDB，CSV 只读一次加载到临时表，列类型 Python 层推断，大文件性能提升 10-20 倍
- **GBK 编码支持**：CSV 文件上传时自动检测编码，GBK 流式转 UTF-8 后交付 DuckDB 处理
- **XLSX 缓存复用**：分析阶段的临时 CSV 缓存供后续字段映射导入阶段重用，避免 openpyxl 二次读取
- **备份 Dify Workflow**：主模型 + 复核模型两个独立 Dify Workflow，支持 DeepSeek API 直连兜底
- **SQL 生成优化**：模糊查询默认 `SELECT *` 呈现全部字段

### 未来可能的迭代方向
- 图表可视化
- 多数据源联合查询
- 查询历史与模板保存
- 自定义完整性测试规则
- 更多 AI 模型支持（通义千问、GLM 等）
