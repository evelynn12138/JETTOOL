# 维护说明

## 1. Dify AI 代理配置

### 配置方式（三种选一）

**方式 0（推荐）：API Key 加密**

把 Dify API Key 加密为 `enc:` 密文存储，即使 `.env` 文件泄露也拿不到明文 key。加密密钥 SECRET_KEY 与密文物理分离（不写在 `.env`）。

1. 复制 `.env.example` 为 `.env`，填入真实的 Dify API Key（明文）
2. 运行加密工具：
   ```bash
   python tools/encrypt_env.py
   ```
   脚本会：生成 SECRET_KEY → 写入独立 `secret_key` 文件（权限 600）→ 把 `.env` 中的 Key 加密为 `enc:...` → 从 `.env` 移除 SECRET_KEY
3. 设置 SECRET_KEY（任选其一，**不要写进 `.env`**）：
   - macOS/Linux：`export SECRET_KEY="$(cat secret_key)"`
   - Windows：`set SECRET_KEY=xxx`
   - 或将 `secret_key` 文件与 `.env` 放在同一目录（启动脚本自动注入）
4. 重启应用，验证 Dify 调用正常

> ⚠️ SECRET_KEY 变化后，已加密的 Key 将无法解密（配置项会打警告并禁用 AI）。
> 换主密钥请用 `python tools/encrypt_env.py --reencrypt --secret 新密钥`。

**方式 C（打包分发）：生成 bundle，exe 零配置**

打包成 exe 分发给同事时，用 bundle 把 key 内置进 exe，用户打开即用、无需任何配置：

1. 打包机先确认 `.env` 里有真实 Dify key（明文或 enc: 密文均可）
2. 生成 bundle：
   ```bash
   python tools/make_bundle.py
   ```
   生成 `dify_bundle.enc`（含加密 key + 内置密钥，已 gitignore）
3. 打包：
   - Windows exe：运行 `build_win.bat`（自动把 `dify_bundle.enc` 打进 `_MEIPASS`）
   - Electron 安装包：运行 `node electron/build.js`（自动复制 bundle 到 app_dist）
4. 把产物 exe / 安装包分发给同事，双击即用

> 🔒 **安全说明**：bundle 含密钥，仅限内部分发。exe 可被反编译提取 key（内部工具可接受）。
> GitHub CI 构建的产物**不含真实 key**（占位 bundle，AI 需用户另行配置）——CI 环境不持有公司 key。

**方式 A：`.env` 文件（明文，兼容旧配置）**

项目根目录创建 `.env` 文件（参考 `.env.example`）：

```bash
DIFY_MAIN_BASE_URL=https://ai-platform-uat.ey.net/v1
DIFY_MAIN_API_KEY=app-your-main-api-key
DIFY_REVIEW_BASE_URL=https://ai-platform-uat.ey.net/v1
DIFY_REVIEW_API_KEY=app-your-review-api-key
```

`.env` 已加入 `.gitignore`，Key 不会上传到代码仓库。明文仍兼容，但不推荐（建议升级为方式 0 加密）。

**方式 B：系统环境变量**

在终端中 export，或在部署脚本中设置：

```bash
export DIFY_MAIN_BASE_URL="https://ai-platform-uat.ey.net/v1"
export DIFY_MAIN_API_KEY="app-your-main-api-key"
export DIFY_REVIEW_API_KEY="app-your-review-api-key"
```

### 故障排查

| 启动日志提示 | 原因 | 处理 |
|------------|------|------|
| `[错误] ... 无法解密` | SECRET_KEY 缺失或不匹配 | 按方式 0 第 3 步正确配置 SECRET_KEY |
| `[警告] SECRET_KEY 写在 .env 中` | 密钥与密文同源，加密失效 | 将 SECRET_KEY 移出 .env，改系统环境变量/secret_key 文件 |
| `[警告] 未检测到 SECRET_KEY` | 明文 Key 可继续用；加密 Key 无法解密 | 加密 Key 需按方式 0 配置 SECRET_KEY |

### 配置项说明

| 变量 | 用途 | 对应 Dify Workflow |
|------|------|-------------------|
| `DIFY_MAIN_BASE_URL` + `DIFY_MAIN_API_KEY` | SQL 生成、字段映射、报表清洗、AI 差异分析等 | 主 Workflow（Qwen3-235B-A3B） |
| `DIFY_REVIEW_BASE_URL` + `DIFY_REVIEW_API_KEY` | SQL 代码复核审查 | 复核 Workflow（Qwen3-30B-A3B） |

### 获取新 API Key

1. 登录 Dify 平台工作流页面
2. 进入「API 访问」页
3. 复制以 `app-` 开头的 API Key

## 2. 文件上传限制

文件大小上限在 `config.py` 中：

```python
MAX_CONTENT_LENGTH = 4096 * 1024 * 1024  # 4GB
```

按需调整数字即可（单位：字节）。

## 3. 预设筛选规则

规则存储在 `temp/preset_rules.json`，JSON 格式。可直接编辑该文件增删规则。每条规则结构：

```json
{
  "id": "rule_id",
  "name": "规则名称",
  "description": "规则描述",
  "category": "分类",
  "sql_template": "SELECT * FROM \"data\" WHERE ...",
  "params": [
    {"name": "param_name", "label": "参数标签", "type": "text"}
  ]
}
```

## 4. PyInstaller 打包

运行 `electron/build.js` 或直接用 PyInstaller：

```bash
pyinstaller --onefile --add-data "templates:templates" --add-data "static:static" run.py
```

打包后的应用数据目录在 `~/.da-cleaner/`。
