# JET数据处理工具 — 产品宣讲 + Demo 演示脚本

> **总时长**: 约 5 分钟  
> **语速**: 正常匀速  
> **结构**: Part 1 痛点引入（1min）→ Part 2 Demo 演示（4min）

---

## Part 1 — 痛点引入（约 1 分钟）

**0:00 - 0:20**
我们 DDP/TRDA 组，每天都会收到大量来自项目组的数据处理需求——序时账和科目余额表的完整性验证、tie报表、需要清洗样本池的控制测试/细节测试、以及最常见的异常凭证筛选。其中大部分数据清洗类需求都需要我们打开 Alteryx、配置工作流、跑一遍、导出来、发回去。项目组看完说再加一个条件，再来一遍。

**0:20 - 0:40**
问题不是我们的现有工具不够强，而是这些简单需求占用了我们太多时间。财审团队一个"筛一下某某科目数据"的请求，从沟通到交付平均半个到一个小时，其中真正打开 Alteryx 干活的时间不到十分钟。剩下的时间全部花在启动Excel/Alteryx、沟通、文件传输上。

**0:40 - 1:00**
JET数据处理工具做的事情很简单——把项目组最常提出的那些数据处理需求以及最常见的JETB完整性测试，从 Alteryx、Excel、Teams 之间的反复切换中解放出来，整合到一个浏览器界面里，变成一个可以即开即用的工具。

---

## Part 2 — Demo 演示（约 4 分钟）

### 第一步：上传文件（1:00 - 1:25）
大家好，今天给大家演示JET数据处理工具，一款本地部署、AI 驱动的财务数据查询分析工具。整个流程分为四步，我们先从上传文件开始

这里支持上传序时账和科目余额表两种数据，格式支持 xlsx 和 csv。先上传序时账文件，可以选择对应的 Sheet，也可以指定表头行。再上传科目余额表，同样的操作步骤，系统会自动解析结构。单个文件最大支持 4GB。两个文件都上传完成后，点击下一步进入字段映射。

### 第二步：字段映射（1:25 - 2:00）

字段映射是将原始数据列对应到系统标准字段。系统已经根据列名做了自动推荐——比如生效日期自动匹配到日期、科目编码自动匹配到科目编号，我们只需要核对一遍。

如果自动匹配不够准确，可以点击 AI 智能映射按钮，AI 会根据列名、数据类型和样本值重新推荐最合适的映射关系。当然也可以手动调整，直接下拉选择源字段。

确认无误后点击保存，系统自动将数据导入到 DuckDB 引擎中，同时引擎会自动识别科目余额表的金额处理方式。

### 第三步：完整性测试（2:00 - 3:00）

进入完整性测试页面，我们把它设计成了 4 步引导流程。

页面顶部可以看到系统给出的 AI 智能建议——它自动分析了数据特征，比如提示我们凭证号有空值、科目余额表科目编号有父子层级等，辅助用户决策要不要开启某些选项。

第一步预处理：如果源数据只在凭证首行有凭证号和日期，可以开启向下填充，系统会先填充再做剔除，避免误删有效数据。清洗规则默认已勾选剔除空凭证行和合计行。

第二步金额处理：系统已根据字段映射自动识别了处理方式。如果序时账映射了方向和金额字段，可以开启借正贷负方向调整。

第三步特殊调整：可以启用反结转——系统默认按本年利润科目识别损益结转凭证，也支持自行修改科目编号和摘要中关键词。如果科目余额表有父子层级，可以开启仅用末级科目。

第四步运行检查：确认配置后，点击运行全部三项检查。结果出来了——序时账完整性、科目余额表完整性、两表交叉验证。每一项都有详细统计，有差异的可以查看明细。需要的话可以点击 AI 差异分析，让 AI 帮忙分析差异原因，也可以导出为 Excel 报告。

### 财务报表清洗 & 科目余额表核对（3:00 - 3:30）

同一页面下方还有财务报表清洗功能。上传资产负债表或利润表，AI 自动识别报表结构，规则引擎自动清洗，输出标准化数据，支持多张报表合并。清洗完成后可以进入科目余额表核对。左侧是每条科目记录，系统已按规则引擎自动映射到报表项目。右侧是核对结果，金额对比和差异一目了然。如果差异较多，可以点击一键优化——系统会通过 AI 加算法自动调整映射方案，最小化总差异。也可以直接在左侧手动修改映射，实时刷新重算。核对结果可以导出为 Excel。

### 第四步：查询分析（3:30 - 4:45）

最后是查询分析，这是日常使用频率最高的功能。可以用中文直接查数据，也可以用预设筛选规则快速查询。

系统内置了三个规则包——通用筛选、行业异常特征、EY GAM 异常凭证筛选。比如选一个大额交易检测，填入金额阈值，点击应用，规则自动填入查询框。

点击生成并执行，主模型 AI 生成 SQL 并执行，同时核查模型会自动进行四维复核——语法检查、安全审查、意图匹配、性能优化。复核结果以折叠面板展示，每个维度都有通过或需关注的状态说明。

也可以自己输入自然语言查询——比如筛选出摘要中包含调整的凭证。点击优化表达，AI 会把模糊描述优化成更具体的表达，比如把"调整"扩展为"调整、adj、adjustment 等相关关键词"。确认后生成并执行，结果即时展示。

有 SQL 基础的同事还可以点击编辑 SQL，在代码编辑器里手动修改 WHERE 条件，灵活调整查询逻辑。

每一条有用的查询可以点击收藏，方便后续复用。查询结果支持批量导出为 Excel文件，便于进一步整理或发给项目组。

### 结尾（4:45 - 5:00）

以上就是 JET数据处理工具当前版本的完整演示。后续我们会在两个方向上继续迭代：一是让工具更智能——项目组输入行业和项目类型，就能自动推荐对应的测试范围和异常凭证分录的criteria；二是让工具更通用——不只做JE数据，也能处理 POS 数据等其他场景的清洗需求。感谢观看。
