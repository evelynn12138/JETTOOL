# 老板汇报串词

## 第二部分：工具概览 — 多功能集成（1 分钟）

> **"这个工具不是一个单一功能的小工具，它是一个多功能平台，目前已经集成了 47 项功能，覆盖 4 个模块。"**

手指屏幕（打开功能清单页面）：

> 这个工具目前覆盖了4大个模块：**数据导入**做序时账和科目余额表上传；**字段映射**把上传后的文件的列名对应到标准字段，这块 可以使用AI 推荐映射关系；**完整性测试**支持JETB的金额完整性验证；**科目余额表和报表核对**使用AI清洗报表后，把TB科目自动对应到财务报表项目，并进行核对；**智能查询**是核心功能，输入筛选条件后查数据，AI 自动写 SQL +做复核

> **"工具的终点是JE筛选，但前面的数据导入、字段映射、完整性测试、报表核对这几个环节，都是它现有的能力。数据导入映射完成后可以跳过中间的完整性校验，直接走到最后一步筛数；也可以一步步走完，保障筛选前的数据质量。"**

---

## 第三部分：当前进度（30 秒）

> **"目前工具的核心功能已经全部开发完成，我们正在做两件事：**
> 
> **第一，代码校验和 bug 修复** —— 把每个功能再跑透，确保稳定性；
> 
> **第二，收集使用反馈** —— 我们在内部试用，看实际场景中哪里好用、哪里需要改。
> 
> **预计 8 月启动二期开发**，二期方向包括图表可视化、多数据源联合查询、自定义完整性测试规则等。"

---

## 第五部分：演示环节（边走边讲，每点 30 秒-1 分钟）

进入演示 → 打开浏览器 → 已运行在 localhost:5003

### 1️⃣ 数据导入

> **"进入工具页面以后首先是序时账和科目余额表的导入，工具读取文件路径后对文件的前15行进行预览，如果序时账文件之前上传过，可以重新加载下之前的映射关系。"**

*操作：上传文件，展示预览界面*

### 2️⃣ 字段映射

> **"上传后进字段映射，左边是源文件的列名，右边选对应的是工具内置的标准字段，这一步的目的是统一字段名称和数据类型后方便后续的完整性测试和筛数工作。这里的映射首先可以根据系统内置规则自动推荐匹配结果，但也可以使用AI看样本数据进行综合推断。最后人工兜底，可以自行修改映射关系，或者手动填充。映射好以后一键保存。"**

*操作：展示映射界面，JETB都需要输入公司字段，点一下AI 映射*

### 3️⃣ 完整性测试

> **"接下来可以跑完整性测试。首先可以根据最上方的JETB的清洗建议，配置相关功能参数，一键运行三项检查：是否有单边凭证、TB是否是平的、JETB的发生额校验。也可以使用AI对差异原因进行分析。"**

接下来可以上传资产负债表以及利润表，AI 会读取前 10 行自动判断报表类型，识别哪些列是项目名、哪些是金额、期初期末怎么分的。最后可以把资产负债表和利润表分别导入后合并到一起，形成结构化的标准数据。

报表核对这里是基于底层 100 多条规则引擎做匹配，匹配不上的 AI 兜底。左边改映射以后右边实时刷新金额对比；一键优化用爬山算法自动调映射让总差异最小化；也有差异 AI 分析原因。也可以手动进行修改后刷新核对，最后导出带差异标识的 Excel。

*操作：点进完整性测试，简单展示配置和结果*

### 4️⃣ 智能查询（重点演示）

> **"最后是核心功能筛数。可以直接在输入框里说筛选条件，比如：'筛选出摘要包含调整的整笔凭证'。可以点击优化表达，AI会帮忙扩展模糊的语义，点击查询后会生成对应的 SQL并执行，另一个模型从语法安全意图性能四个角度进行复核，几秒出结果。也可以点击内置筛选条件，进行筛选。"**

*操作：输入一条查询，展示结果*

> **"如果结果不满意，可以直接在编辑器里改 SQL，也有查历史、收藏查询的功能。"**

---

## 第六部分：收尾（30 秒）

> **"总结一下这个工具的定位：**
> 
> - ✅ **本地部署** —— 数据不上云，安全可控
> - ✅ **零代码** —— 财务人员说中文就能查
> - ✅ **多环节覆盖** —— 不止查询，导入、映射、验证、清洗全覆盖
> - ✅ **AI 驱动** —— 从映射到查询到复核，AI 贯穿全线
> - ✅ **高性能** —— DuckDB 引擎，百万行数据秒级处理
> 
> **目前在代码打磨和反馈收集阶段，8 月做二期。老板你看有什么问题？"**

---

## 附录：演示时可能被问到的问题

**Q：这个工具和 Excel 有什么区别？**
> A：Excel 每次查询都要手动筛选、透视、公式，一个简单问题 5-15 分钟。这个工具说一句话几秒钟出结果，而且自动做完整性验证和交叉核对。

**Q：数据安全怎么保证？**
> A：所有数据在本地处理，不上传任何第三方服务器。AI 调用通过 Dify 代理，只传字段名和 SQL，不传原始数据。每个浏览器会话独立数据库文件，隔离安全。

**Q：AI 不准怎么办？**
> A：两个机制兜底：一是 AI 生成的 SQL 有第二个模型做四维复核；二是用户可以手动编辑 SQL。目前首次执行成功率在 70% 以上，后续通过反馈持续优化。

**Q：和安永现有的工具什么关系？**
> A：这是针对国内审计场景做的轻量补充，侧重自然语言查询和数据完整性验证，和现有方法论（如 GAM 异常筛选规则）是兼容互补的。
